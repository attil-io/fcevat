CREATE TABLE IF NOT EXISTS messageentity (ID integer primary key, TEXT varchar);
DELETE FROM messageentity;

INSERT INTO messageentity VALUES(1, 'hello');
INSERT INTO messageentity VALUES(2, 'world');
